# LinkFile API

FastAPI backend for LinkFile online mode. It owns authentication, metadata APIs, encrypted storage method management, sharing, redirect/proxy download endpoints, sync, and server-side local-server storage.
